experiment:
  name: residual_input_dependence_v001
  seq_len: 128
  batch_size: 8
  num_chunks: 10000
  device: auto

data:
  source: wikitext
  split: train
  revision: b08601e

activations:
  hook_name: resid_post

outputs:
  root: results

snapshots:
  enabled: true
  points: [100, 500, 1000, 5000, 10000]

reliability:
  split_half: true
  seed: 0
