experiment:
  name: residual_input_dependence_v003
  seq_len: 128
  batch_size: 16
  num_chunks: 10000
  device: auto

data:
  source: wikitext
  split: train
  revision: b08601e
  sample_seed: 44

activations:
  targets: [resid_post, resid_delta, attn_out, mlp_out]

classification:
  inactive_energy_ratio: 0.01
  consistent_ivr_max: 0.1
  varying_ivr_min: 0.5

outputs:
  root: results

snapshots:
  enabled: false
  points: []

reliability:
  split_half: false
  seed: 0
