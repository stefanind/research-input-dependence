experiment:
  name: residual_input_dependence_v001
  seq_len: 128
  batch_size: 8
  num_chunks: 1000 # increase to 10000 after this works
  device: cuda

data:
  source: wikitext
  split: train

activations:
  hook_name: resid_post

outputs:
  stats_dir: results/stats
  metrics_dir: results/metrics
  figures_dir: results/figures

snapshots:
  enabled: true
  points: [100, 500, 1000]